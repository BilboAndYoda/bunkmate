<div class="form-signin" >
<div class="w3-container w3-section w3-border w3-round-xlarge">
<h2> This is where the form will go for the student to fill out </h2>
<h3> Users in database: </h3>
<% @users.each do |user| %>
  <p><%= user.email %> </p>
<% end %>
<li><%= link_to "Sign out", destroy_user_session_path, data: { confirm: "Are you sure?" }, :method => :delete %></li>
</div>
</div>